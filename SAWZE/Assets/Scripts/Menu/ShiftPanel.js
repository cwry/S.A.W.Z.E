#pragma strict

var mainPanel : RectTransform; 
var levelPanel : RectTransform;

var mainRight : float;
var mainLeft : float;
var levelRight : float;
var levelLeft : float;

function Update(){
	/*mainPanel.right = Mathf.Lerp(mainPanel.right., mainRight, Time.deltaTime);
	mainPanel.left = Mathf.Lerp(mainPanel.left, mainLeft, Time.deltaTime);
	levelPanel.right = Mathf.Lerp(levelPanel.right, levelRight, Time.deltaTime);
	levelPanel.left = Mathf.Lerp(levelPanel.left, levelLeft, Time.deltaTime);*/
	
}